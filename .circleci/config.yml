version: 2
jobs:
  test:
    docker:
      - image: kkarczmarczyk/node-yarn:8.0
    steps:
      - checkout
      - run: yarn
      - run: yarn test
workflows:
  version: 2
  test_and_build:
    jobs:
      - test
